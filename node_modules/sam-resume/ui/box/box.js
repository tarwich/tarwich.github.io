const app = require('sam-resume/app');

app.directive('uiBox', () => ({
  restrict:    'E',
  replace:     true,
  transclude:  true,
  templateUrl: 'ui/box/box.html',
  scope:       {
    header: '@',
    title:  '@',
  },
}));
